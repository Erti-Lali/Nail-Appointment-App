EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Web backend URL (booking availability/create, push token registration).
# Device testing: use the dev machine's LAN IP (localhost does NOT work on a phone),
# e.g. http://192.168.1.20:3010 ; in production use the deployed web URL.
EXPO_PUBLIC_API_URL=
